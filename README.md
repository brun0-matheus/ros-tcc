# ROS attacks - TCC

Para compilar, roda `make NOME_DO_BINARIO`.

Por enquanto, tem os seguintes binários:

`main`: POC de geração de assinatura cliente-servidor.
`benchmark`: Benchmark do esquema de blind signature.

Por hora não implementei os ataques.

