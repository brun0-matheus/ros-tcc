# ROS attacks - TCC

Para compilar, roda `make NOME_DO_BINARIO`.

Por enquanto, tem os seguintes binários:

`test_bs`: POC de geração de assinatura cliente-servidor.
`main`: POC do ros, com medição de tempo (conta também o tempo de execução do servidor).

Por hora só foi implementado o ROS normal, sem redução de dimensionalidade.

